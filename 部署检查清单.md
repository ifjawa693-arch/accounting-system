# ✅ 部署检查清单

按照这个清单逐步检查，确保每个步骤都完成。

## 📋 部署前准备

- [ ] 确认服务器IP地址
- [ ] 确认SSH登录信息（用户名、密码或密钥）
- [ ] 确认服务器操作系统（Ubuntu/CentOS）
- [ ] 本地代码可以正常运行

## 🔧 服务器环境

- [ ] 已连接服务器（SSH）
- [ ] Node.js已安装：`node --version` （应该是 v18.x 或更高）
- [ ] npm已安装：`npm --version`
- [ ] Git已安装（如果使用Git）：`git --version`

## 📤 代码上传

- [ ] 项目文件已上传到服务器
- [ ] 文件位置：`/var/www/accounting-system`（或你选择的位置）
- [ ] 文件权限正确：`ls -la` 可以看到文件

## 📦 依赖安装

- [ ] 前端依赖已安装：`cd /var/www/accounting-system && npm install`
- [ ] 前端构建成功：`npm run build`（生成了 `dist` 文件夹）
- [ ] 后端依赖已安装：`cd backend && npm install`

## 🔧 配置修改

- [ ] API地址已配置（`src/services/api.ts` 使用相对路径 `/api`）
- [ ] 已重新构建前端：`npm run build`

## 🚀 PM2配置

- [ ] PM2已安装：`pm2 --version`
- [ ] 后端服务已启动：`pm2 start server.js --name accounting-api`
- [ ] PM2状态正常：`pm2 status` 显示 `online`
- [ ] PM2已设置开机自启：`pm2 startup` 和 `pm2 save`

## 🌐 Nginx配置

- [ ] Nginx已安装：`nginx -v`
- [ ] Nginx配置文件已创建：`/etc/nginx/sites-available/accounting`
- [ ] 配置文件内容正确（前端路径、API代理）
- [ ] 配置已启用：`sudo ln -s /etc/nginx/sites-available/accounting /etc/nginx/sites-enabled/`
- [ ] 配置测试通过：`sudo nginx -t` 显示 `test is successful`
- [ ] Nginx已启动：`sudo systemctl status nginx` 显示 `active (running)`

## 🔥 防火墙配置

- [ ] 服务器防火墙已配置（开放80、443端口）
- [ ] 网易云安全组已配置（开放80、443端口）

## ✅ 功能测试

- [ ] 可以访问网站：`http://你的服务器IP`
- [ ] 页面正常显示（没有404或空白页）
- [ ] 可以添加客户
- [ ] 可以添加供应商
- [ ] 可以添加员工
- [ ] 刷新页面后数据还在（数据库持久化正常）
- [ ] 浏览器控制台（F12）没有错误

## 📊 日志检查

- [ ] 后端日志正常：`pm2 logs accounting-api` 没有错误
- [ ] Nginx访问日志正常：`sudo tail /var/log/nginx/access.log`
- [ ] Nginx错误日志正常：`sudo tail /var/log/nginx/error.log` 没有错误

## 🎯 最终验证

- [ ] 所有功能正常
- [ ] 数据可以保存
- [ ] 页面刷新数据不丢失
- [ ] 可以正常使用系统

---

## 🐛 如果出现问题

1. **无法访问网站**
   - 检查Nginx：`sudo systemctl status nginx`
   - 检查PM2：`pm2 status`
   - 检查防火墙：`sudo ufw status`

2. **API请求失败**
   - 检查后端：`pm2 logs accounting-api`
   - 测试API：`curl http://localhost:3001/api/health`
   - 检查API配置：`cat src/services/api.ts`

3. **数据不保存**
   - 检查数据库文件权限：`ls -la backend/accounting.db`
   - 检查后端日志：`pm2 logs accounting-api`

---

完成所有检查项后，系统应该可以正常使用了！🎉

